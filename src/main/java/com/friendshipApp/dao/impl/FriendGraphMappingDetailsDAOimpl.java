package com.friendshipApp.dao.impl;

import org.springframework.stereotype.Repository;

@Repository("friendGraphMappingDetailsDAO")
public class FriendGraphMappingDetailsDAOimpl 
{

	/*public FriendGraphMappingDetails storeGraph(FriendGraphMappingDetails friendGraphMappingDetails) 
	{
		EntityManagerFactory emf =
                Persistence.createEntityManagerFactory("example-unit");
		
		EntityManager em = emf.createEntityManager();
		
		em.getTransaction().begin();
        em.persist(friendGraphMappingDetails);
        em.getTransaction().commit();
        em.close();
		
        return friendGraphMappingDetails;
	}*/

}
